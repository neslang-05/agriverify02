# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json
name: fake-seed-detection
metadata:
  template: fake-seed-detection@0.0.1
services:
  fake-seed-detection:
    project: .
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .